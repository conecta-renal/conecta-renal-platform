name: Terraform

on:
  pull_request:
    branches:
      - main
    paths:
      - "infra/**"
  push:
    branches:
      - main
    paths:
      - "infra/**"

# Serializa todas as execucoes deste workflow (plan e apply, PRs e pushes).
# Sem isso, dois disparos proximos (ex: push seguido de outro push, ou PR
# atualizado durante um apply em andamento) podem rodar em paralelo contra
# o mesmo state remoto; se o GitHub cancelar uma execucao no meio do
# apply, o Terraform nao chega a liberar o lock do blob, deixando o state
# travado ("state blob is already locked") para a proxima execucao.
# cancel-in-progress: false garante que novos disparos esperem na fila em
# vez de cancelar uma execucao em andamento.
concurrency:
  group: terraform-conecta-renal-infra
  cancel-in-progress: false

env:
  ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
  ARM_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
  ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
  ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

defaults:
  run:
    working-directory: infra

jobs:
  plan:
    name: Terraform Plan
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform Format Check
        run: terraform fmt -check -recursive

      - name: Terraform Init
        run: |
          terraform init \
            -backend-config="resource_group_name=rg-conecta-renal-tfstate" \
            -backend-config="storage_account_name=stconectarenaltf" \
            -backend-config="container_name=tfstate" \
            -backend-config="key=conecta-renal.tfstate"

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Plan
        run: terraform plan -input=false
        env:
          TF_VAR_subscription_id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          TF_VAR_tenant_id: ${{ secrets.AZURE_TENANT_ID }}
          TF_VAR_client_id: ${{ secrets.AZURE_CLIENT_ID }}
          TF_VAR_client_secret: ${{ secrets.AZURE_CLIENT_SECRET }}

  apply:
    name: Terraform Apply
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform Format Check
        run: terraform fmt -check -recursive

      - name: Terraform Init
        run: |
          terraform init \
            -backend-config="resource_group_name=rg-conecta-renal-tfstate" \
            -backend-config="storage_account_name=stconectarenaltf" \
            -backend-config="container_name=tfstate" \
            -backend-config="key=conecta-renal.tfstate"

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Apply
        run: terraform apply -auto-approve -input=false
        env:
          TF_VAR_subscription_id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          TF_VAR_tenant_id: ${{ secrets.AZURE_TENANT_ID }}
          TF_VAR_client_id: ${{ secrets.AZURE_CLIENT_ID }}
          TF_VAR_client_secret: ${{ secrets.AZURE_CLIENT_SECRET }}
